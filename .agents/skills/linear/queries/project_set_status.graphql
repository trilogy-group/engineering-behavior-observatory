mutation SetProjectStatus($id: String!, $statusId: String!) {
  projectUpdate(id: $id, input: { statusId: $statusId }) {
    success
    project {
      id
      name
      status {
        id
        name
      }
    }
  }
}
